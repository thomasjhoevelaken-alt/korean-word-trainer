const cacheName='korean-word-trainer-v1';const filesToCache=['./korean_word_trainer.html','./manifest.json','./icon.png'];self.addEventListener('install',evt=>{evt.waitUntil(caches.open(cacheName).then(cache=>cache.addAll(filesToCache)));});self.addEventListener('fetch',evt=>{evt.respondWith(caches.match(evt.request).then(resp=>resp||fetch(evt.request)));});